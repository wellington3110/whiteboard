apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  labels:
    team: backend
spec:
  ports:
    - name: go-app
      port: 8080
      targetPort: go-app
      protocol: TCP
  selector:
    app: my-app
